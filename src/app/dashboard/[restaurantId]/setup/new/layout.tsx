import { createPrivateMetadata } from '@/lib/seo'

export const metadata = createPrivateMetadata('Create Brand', 'Add another brand or property to your workspace portfolio.')

export default function NewDashboardBrandLayout({ children }: { children: React.ReactNode }) {
  return children
}